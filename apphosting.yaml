# Firebase App Hosting is not required for this static Jaspr site.
# Keep this file for future backend runtime use only.
# Static deployment is handled via Firebase Hosting with:
# - build command: dart run jaspr_builder build
# - output directory: build/jaspr

runConfig:
  minInstances: 0
